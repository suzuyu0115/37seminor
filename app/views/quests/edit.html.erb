<% content_for(:title, t('.title')) %>
<div class="w-full max-w-lg mx-auto my-10 bg-white p-4 rounded-xl shadow shadow-slate-300 bg-opacity-75">
  <h1 class="font-bold text-4xl mt-10"><%= t('.title') %></h1>

  <%= render 'quests/form', { quest: @quest } %>
</div>
